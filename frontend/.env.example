# Empty keeps same-origin /api calls for the local FastAPI-served build.
# A Vercel deployment must use the public HTTPS origin of the backend, with no trailing slash.
VITE_API_BASE_URL=https://api.example.com
